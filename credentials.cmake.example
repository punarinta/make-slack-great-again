# Copy this file to credentials.cmake and fill in your Slack app credentials.
# credentials.cmake is gitignored — never commit the real values.
set(MSGA_CLIENT_ID     "")
set(MSGA_CLIENT_SECRET "")
set(MSGA_XAPP          "")
